# roman-borders

This simple Python app generates a border, in SVG according to what I believe is a classic Roman pattern. 

You can change parameters such as width, height, margin and the spacing between the lines.

To run, simply use:

./roman-borders.py

Then view roman-borders.svg
